<% @backers.each do |backer| %>
  <% unless backer.backer_documents.select{|rd| rd.document.title == file && rd.document.destroy_by_user == nil }.empty? %>
    <i class="fas fa-check-circle font-green"></i>
  <% else %>
    <i class="fas fa-times"></i>
  <% end %>
<% end %>
